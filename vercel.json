{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "rewrites": [
    {
      "//": "Only rewrite requests that do NOT contain a file extension. This preserves access to static assets like js/, css/, images, etc.",
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}